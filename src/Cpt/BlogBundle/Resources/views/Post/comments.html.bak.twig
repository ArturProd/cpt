{#----------------------------------------------------------------------------#}
{#----------------------------------------------------------------------------#}
{# Displays and manages a list of post comments
{#----------------------------------------------------------------------------#}
{#----------------------------------------------------------------------------#}
{% set NB_COMMENTS_PRELOADED = 3 %}
{% set NB_COMMENTS_SEEMORECOMMENTS = 5 %}
{##############################################################################}
{# JAVASCRIPT
{##############################################################################}
<script type="text/javascript">
{#****************************************************************************#}
{# OnLoad
{#****************************************************************************#}
    $(function() {
        {#****************************************************************************#}
        {# Get preloaded comments
        {#****************************************************************************#}
        get_more_comments({{post.id}}, {{NB_COMMENTS_PRELOADED}}, function(comments){
            no_more_older_comments({{post.id}},comments.length ); {# disable link to get more comment if there are no more #}
        });
        {#****************************************************************************#}
        {# Make comment submission form Ajax 
        {#****************************************************************************#}
        AjaxForm("{{'form_comment_'~post.id}}", null, comment_submit_callback);
        {#****************************************************************************#}
        {# Handles clicks to get more comments 
        {#****************************************************************************#}
        $('#btn_more_comments_{{ post.id }}').on("click", function(){
            if (!isDisabled($(this))) {# only if the link is not disables #}
            { 
                EnableControl($(this),false); {# Disable the link #}
                {# get more comments #}
                get_more_comments({{post.id}}, {{NB_COMMENTS_SEEMORECOMMENTS}}, function(comments){ 
                    {# in case of success re-enable the link and check if there are more comments to come  #}
                    EnableControl($('#btn_more_comments_{{ post.id }}'),true);
                    no_more_older_comments({{post.id}},comments.length );
                });
             }
        });
        {#****************************************************************************#}
        {# You can only submit a comment if you entered something in the texterea
        {#****************************************************************************#}
       {# Toggle the button on key up #}
       $('#form_comment_{{post_id}}').find('#comment_message').keyup(function(){
           var textvalue = $('#form_comment_{{post_id}}').find('#comment_message').val();
           toggle_submit_comment_button(textvalue,{{post_id}});
       });
       {# Toggle the button on change #}
       $('#form_comment_{{post_id}}').find('#comment_message').change(function(){
           var textvalue = $('#form_comment_{{post_id}}').find('#comment_message').val();
           toggle_submit_comment_button(textvalue,{{post_id}});
       });
       
       {#****************************************************************************#}
       {# Check for new comments
       {#****************************************************************************#}      
       setTimeout("get_new_comments({{post_id}})", TIMEOUT_COMMENT_CHECK_MS);
    });
</script>
{##############################################################################}
{# HTML
{##############################################################################}
<div id="comment_section{{ post.id }}" class="comments">
{#****************************************************************************#}
{# Comment submission form
{#****************************************************************************#}
{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
{{ form_start(commentform, {'action': path('sonata_news_add_comment',  { 'postid': post.id }), 'method': 'POST', 'attr': {'id': 'form_comment_'~post.id } }  ) }}
<div class="comment_form">
        {{ form_widget(commentform.message, { 'id': 'form_comment_textarea'~post.id }) }}
        {{ form_errors(commentform.message) }}

        {{ form_rest(commentform) }}
        <input type="submit" value="{{'btn_add_comment'|trans }}" id="submit_comment_{{post_id}}" class="btn_green"/>
</div>
 {{form_end(commentform) }} 
{% endif %}
{#****************************************************************************#}
{# List of comments
{#****************************************************************************#}
    <ul class="comment" id="comment_list_{{post_id}}">
    </ul>
     <div id="btn_more_comments_wrapper_{{ post.id }}" class="more_comments_wrapper"><a id="btn_more_comments_{{ post.id }}">Voir plus de commentaires</a></div>
</div>